import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'bipclusion#2-designify',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
